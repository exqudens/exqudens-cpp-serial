# exqudens-cpp-serial

##### How-To-Export

```bash
rm -rf build
cmake --list-presets
cmake --preset <preset>
cmake --build --preset <preset>
cmake --build --preset <preset> --target conan-export
```
